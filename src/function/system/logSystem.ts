import createSystem from "./utils/createSystem";

export const logSystem = createSystem('logSystem', {
  update: (queued, a) => {

    // for (const iterator of queued) {
    //   // console.log()
    //   // iterator.n += 0.1
    // }
  },
})